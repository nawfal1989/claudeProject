name: Claude

on:
  push:
    branches: [main]

jobs:
  claude:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Claude
        run: echo "Claude workflow running"
